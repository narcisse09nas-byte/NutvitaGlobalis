import AdminShell from "@/components/admin/AdminShell";import DietitianManager from "@/components/admin/DietitianManager";import {requireAdmin} from "@/lib/admin";
export default async function DietitiansPage(){const {supabase,admin}=await requireAdmin();const {data}=await supabase.from('dietitian_profiles').select('*').order('created_at',{ascending:false});return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><h1 className="text-3xl font-black">Diététiciens partenaires</h1><p className="mt-2 text-slate-500">Profils créés après l’intégration finale.</p></div><DietitianManager initial={data||[]}/></AdminShell>}
