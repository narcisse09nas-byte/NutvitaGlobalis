import AdminShell from "@/components/admin/AdminShell";import SystemEmailManager from "@/components/admin/SystemEmailManager";import {requireAdmin} from "@/lib/admin";
export default async function SystemEmailsPage(){const {supabase,admin}=await requireAdmin();const [{data:templates},{data:logs}]=await Promise.all([supabase.from('system_email_templates').select('*').order('name'),supabase.from('system_email_logs').select('*').order('created_at',{ascending:false}).limit(100)]);return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><h1 className="text-3xl font-black">Emails systeme</h1><p className="mt-2 text-slate-500">Modeles transactionnels et journal des envois.</p></div><SystemEmailManager templates={templates||[]} logs={logs||[]}/></AdminShell>}
