import AdminShell from "@/components/admin/AdminShell";import InvoiceManager from "@/components/admin/InvoiceManager";import {requireAdmin} from "@/lib/admin";
export default async function InvoicesPage(){const {supabase,admin}=await requireAdmin();const {data}=await supabase.from("invoices").select("*").order("issued_at",{ascending:false});return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><h1 className="text-3xl font-black">Factures</h1><p className="mt-2 text-slate-500">Telechargement, recherche et renvoi par email.</p></div><InvoiceManager initial={data||[]}/></AdminShell>}
