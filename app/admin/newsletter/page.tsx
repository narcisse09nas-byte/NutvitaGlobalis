import AdminShell from "@/components/admin/AdminShell";import NewsletterTable from "@/components/admin/NewsletterTable";import {requireAdmin} from "@/lib/admin";
export default async function Page(){const {supabase,admin}=await requireAdmin();const {data}=await supabase.from('newsletter_subscribers').select('*').order('subscribed_at',{ascending:false});return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><h1 className="text-3xl font-black">Newsletter</h1><p className="mt-2 text-slate-500">Abonnés et sources d’inscription.</p></div><NewsletterTable rows={data||[]}/></AdminShell>}
