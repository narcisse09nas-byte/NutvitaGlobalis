import AdminShell from "@/components/admin/AdminShell";import PaymentManager from "@/components/admin/PaymentManager";import {requireAdmin} from "@/lib/admin";
export default async function PaymentsPage(){const {supabase,admin}=await requireAdmin();const {data}=await supabase.from("payments").select("*, client_profiles(full_name,email)").order("created_at",{ascending:false});return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><h1 className="text-3xl font-black">Paiements</h1><p className="mt-2 text-slate-500">Transactions Stripe, Flutterwave et suivi des statuts.</p></div><PaymentManager initial={data||[]}/></AdminShell>}
