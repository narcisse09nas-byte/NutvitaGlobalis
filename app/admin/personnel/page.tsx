import AdminShell from "@/components/admin/AdminShell";import StaffManager from "@/components/admin/StaffManager";import {requireAdmin} from "@/lib/admin";
export default async function Page(){const {supabase,admin}=await requireAdmin();const {data}=await supabase.from('staff_profiles').select('*').order('created_at',{ascending:false});return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><h1 className="text-3xl font-black">Personnel et départements</h1><p className="mt-2 text-slate-500">Administration, santé, finance, technologie, opérations et futur service de restauration.</p></div><StaffManager initial={data||[]}/></AdminShell>}
