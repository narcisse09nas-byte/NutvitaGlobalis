import AdminShell from "@/components/admin/AdminShell";import RecruitmentManager from "@/components/admin/RecruitmentManager";import {requireAdmin} from "@/lib/admin";
export default async function Page(){const{supabase,admin}=await requireAdmin();const[{data:apps},{data:questions}]=await Promise.all([supabase.from("recruitment_applications").select("*, recruitment_test_attempts(*)").eq("recruitment_type","promoter").in("status",["invited_to_interview","interview_completed","selected","rejected","integrated"]).order("updated_at",{ascending:false}),supabase.from("recruitment_test_questions").select("*").eq("category","Promoteur").order("position")]);return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><p className="text-xs font-black uppercase tracking-widest text-orange">Recrutement promoteurs</p><h1 className="mt-2 text-4xl font-black">Décisions et codes promotionnels</h1><p className="mt-2 text-slate-500">Finalisez l'évaluation. L'intégration active le profil et attribue automatiquement le matricule/code du promoteur.</p></div><RecruitmentManager initial={apps||[]} questions={questions||[]}/></AdminShell>}
