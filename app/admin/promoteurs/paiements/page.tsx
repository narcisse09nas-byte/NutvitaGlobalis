import AdminShell from "@/components/admin/AdminShell";import PromoterManager from "@/components/admin/PromoterManager";import {requireAdmin} from "@/lib/admin";
export default async function Page(){const{supabase,admin}=await requireAdmin();const[{data:promoters},{data:balances},{data:payouts},{data:ledger}]=await Promise.all([supabase.from("promoter_profiles").select("*").eq("status","active").order("full_name"),supabase.from("promoter_balances").select("*"),supabase.from("promoter_payouts").select("*, promoter_profiles(full_name,matricule)").order("created_at",{ascending:false}).limit(100),supabase.from("promoter_ledger").select("*, promoter_profiles(full_name,matricule)").order("created_at",{ascending:false}).limit(100)]);return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><p className="text-xs font-black uppercase tracking-widest text-orange">Finance promoteurs</p><h1 className="mt-2 text-4xl font-black">Paiements et preuves</h1><p className="mt-2 text-slate-500">Sélectionnez le promoteur, saisissez le montant et joignez la preuve du versement.</p></div><PromoterManager promoters={promoters||[]} balances={balances||[]} payouts={payouts||[]} ledger={ledger||[]}/></AdminShell>}
