import AdminShell from "@/components/admin/AdminShell";import PromoterRegistry from "@/components/admin/PromoterRegistry";import {requireAdmin} from "@/lib/admin";
export default async function Page(){const{supabase,admin}=await requireAdmin();const[{data:promoters},{data:balances}]=await Promise.all([supabase.from("promoter_profiles").select("*").order("full_name"),supabase.from("promoter_balances").select("*")]);return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><p className="text-xs font-black uppercase tracking-widest text-orange">Programme promoteurs</p><h1 className="mt-2 text-4xl font-black">Registre des promoteurs</h1><p className="mt-2 text-slate-500">Recherchez, filtrez par tranche de cagnote, consultez les détails ou retirez un promoteur.</p></div><PromoterRegistry initial={promoters||[]} balances={balances||[]}/></AdminShell>}
