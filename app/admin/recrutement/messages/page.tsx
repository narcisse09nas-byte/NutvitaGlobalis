import AdminShell from "@/components/admin/AdminShell";import RecruitmentMessages from "@/components/admin/RecruitmentMessages";import {requireAdmin} from "@/lib/admin";
export default async function RecruitmentMessagesPage(){const {supabase,admin,user}=await requireAdmin();const {data}=await supabase.from('recruitment_applications').select('id,candidate_id,full_name,email,status').order('updated_at',{ascending:false});return <AdminShell name={admin.full_name||admin.email}><div className="mb-7"><h1 className="text-3xl font-black">Messagerie recrutement</h1><p className="mt-2 text-slate-500">Conversations privées avec les candidats.</p></div><RecruitmentMessages applications={data||[]} adminId={user.id}/></AdminShell>}
