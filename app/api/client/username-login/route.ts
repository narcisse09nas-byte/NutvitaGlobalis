import {NextResponse} from "next/server";import {createAdminClient} from "@/lib/supabase/admin";import {createClient} from "@/lib/supabase/server";import {hasSupabaseConfig} from "@/lib/supabase/config";
export async function POST(request:Request){if(!hasSupabaseConfig())return NextResponse.json({message:'Utilisez le compte client local de demonstration.'},{status:400});const {identifier,password}=await request.json();let email=String(identifier).trim().toLowerCase();if(!email.includes('@')){const {data}=await createAdminClient().from('client_profiles').select('login_email').eq('username',email).maybeSingle();if(!data?.login_email)return NextResponse.json({message:'Identifiant inconnu.'},{status:401});email=data.login_email}const supabase=await createClient(),{error}=await supabase.auth.signInWithPassword({email,password});if(error)return NextResponse.json({message:'Identifiant ou mot de passe incorrect.'},{status:401});return NextResponse.json({ok:true})}
