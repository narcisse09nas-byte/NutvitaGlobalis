import Link from "next/link";import {ChatBubbleLeftRightIcon,EnvelopeIcon,MapPinIcon} from "@heroicons/react/24/outline";import ContactForm from "@/components/ContactForm";import ManagedPageHero from "@/components/ManagedPageHero";import ManagedPageSections from "@/components/ManagedPageSections";import {siteConfig} from "@/data/site";import {getSitePage} from "@/lib/site-pages";import {getCurrentLocale} from "@/lib/i18n-server";import {getHomepage} from "@/lib/public-content";
export const metadata={title:"Contact"};type ContactItem={Icon:typeof EnvelopeIcon;label:string;value:string;href?:string};export default async function Contact(){const[page,locale,settings]=await Promise.all([getSitePage("contact"),getCurrentLocale(),getHomepage()]),english=locale==="en",whatsapp=String(settings.whatsapp_group_url||""),items:ContactItem[]=[{Icon:EnvelopeIcon,label:"Email",value:siteConfig.email,href:`mailto:${siteConfig.email}`},...(whatsapp?[{Icon:ChatBubbleLeftRightIcon,label:"Groupe WhatsApp",value:english?"Join the NutVitaGlobalis group":"Rejoindre le groupe NutVitaGlobalis",href:whatsapp}]:[]),{Icon:MapPinIcon,label:english?"Address":"Adresse",value:siteConfig.address}];return <>{page&&<><ManagedPageHero initial={page}/><ManagedPageSections initial={page}/></>}<section className="section"><div className="container-site grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><h2 className="text-3xl font-black">{english?"Contact details":"Coordonnees"}</h2><p className="mt-4 leading-7 text-slate-600">{english?"Send us a message. Reply details are optional.":"Deposez votre message. Les coordonnees de reponse sont facultatives."}</p><div className="mt-8 grid gap-4">{items.map(({Icon,label,value,href})=>{const content=<><span className="grid h-11 w-11 place-items-center rounded-xl bg-mint"><Icon className="h-5 text-leaf"/></span><div><p className="text-xs font-bold uppercase text-slate-400">{label}</p><p className="font-bold text-forest">{value}</p></div></>;return href?<Link href={href} target={href.startsWith("http")?"_blank":undefined} className="flex items-center gap-4 rounded-2xl bg-white p-5" key={label}>{content}</Link>:<div className="flex items-center gap-4 rounded-2xl bg-white p-5" key={label}>{content}</div>})}</div></div><ContactForm/></div></section></>}