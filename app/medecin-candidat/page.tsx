import Link from "next/link";
import {redirect} from "next/navigation";
import {createClient} from "@/lib/supabase/server";
import {getCurrentLocale} from "@/lib/i18n-server";
export default async function Page(){const locale=await getCurrentLocale(),en=locale==="en",supabase=await createClient(),{data:{user}}=await supabase.auth.getUser();if(!user)redirect(`/${locale}/connexion?next=/medecin-candidat`);const[{data:app},{data:notifications},{data:interviews}]=await Promise.all([supabase.from("medical_specialist_applications").select("*").eq("candidate_id",user.id).order("applied_at",{ascending:false}).limit(1).maybeSingle(),supabase.from("medical_specialist_notifications").select("*").eq("user_id",user.id).order("created_at",{ascending:false}),supabase.from("medical_specialist_interviews").select("*,medical_specialist_applications!inner(candidate_id)").eq("medical_specialist_applications.candidate_id",user.id).order("scheduled_at",{ascending:false})]);return <main className="min-h-screen bg-slate-100 py-10"><div className="container-site"><div className="flex justify-between"><div><p className="font-black text-orange">NutVitaCandidate</p><h1 className="mt-3 text-4xl font-black">{en?`Hello ${app?.full_name||"Doctor"}`:`Bonjour ${app?.full_name||"Docteur"}`}</h1></div><Link href="/" className="btn-secondary">{en?"Main site":"Page principale"}</Link></div><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_360px]"><section className="card p-8"><small className="font-black uppercase tracking-widest text-slate-400">{en?"Current status":"Statut actuel"}</small><h2 className="mt-3 text-3xl font-black">{app?.status||"pending"}</h2><p className="mt-3">{app?.application_code}</p><div className="mt-8 grid gap-4">{notifications?.map((n:any)=><article key={n.id} className="rounded-2xl border p-5"><b>{en?n.title_en:n.title_fr}</b><p className="mt-2 text-slate-600">{en?n.message_en:n.message_fr}</p><small>{new Date(n.created_at).toLocaleString(en?"en-GB":"fr-FR")}</small></article>)}</div></section><aside className="grid content-start gap-5"><div className="rounded-3xl bg-forest p-7 text-white"><h2 className="text-white">{en?"My interviews":"Mes entretiens"}</h2>{interviews?.map((i:any)=><div key={i.id} className="mt-4 rounded-xl bg-white/10 p-4"><b>{new Date(i.scheduled_at).toLocaleString(en?"en-GB":"fr-FR")}</b><a href={i.meeting_url} className="mt-3 block underline">{en?"Join the room":"Rejoindre la salle"}</a></div>)}</div><div className="card p-6"><b>{en?"Need help?":"Besoin d'aide ?"}</b><a href="mailto:contact@nutvitaglobalis.com" className="mt-3 block text-leaf">contact@nutvitaglobalis.com</a></div></aside></div></div></main>}
