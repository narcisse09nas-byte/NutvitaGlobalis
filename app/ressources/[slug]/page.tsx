import Image from "next/image"; import Link from "next/link"; import { notFound } from "next/navigation"; import { articles } from "@/data/content"; import { getArticle } from "@/lib/public-content";
export const revalidate=60;
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await getArticle(slug);return {title:a?.seoTitle||a?.title||"Article",description:a?.seoDescription||a?.excerpt};}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await getArticle(slug);if(!a)notFound();return <article><div className="container-site py-12"><Link href="/ressources" className="font-bold text-leaf">← Retour aux ressources</Link><div className="mx-auto mt-10 max-w-4xl text-center"><span className="eyebrow">{a.category}</span><h1 className="text-4xl font-black md:text-6xl">{a.title}</h1><p className="mx-auto mt-5 max-w-2xl text-lg leading-8">{a.excerpt}</p><p className="mt-4 text-sm text-slate-400">Lecture : {a.readTime} · {a.author||'Équipe NutVitaGlobalis'}</p></div><div className="relative mx-auto mt-12 h-[300px] max-w-5xl overflow-hidden rounded-[2rem] md:h-[520px]"><Image src={a.image} alt={a.title} fill priority className="object-cover"/></div>{a.content?<div className="article-content mx-auto max-w-3xl py-14 leading-8" dangerouslySetInnerHTML={{__html:a.content}}/>:<div className="mx-auto max-w-3xl py-14 leading-8"><p>Une bonne alimentation repose sur des choix éclairés, réguliers et adaptés à votre réalité.</p><h2 className="mt-10 text-3xl font-black">Les repères essentiels</h2><p className="mt-4">Privilégiez une alimentation variée, riche en légumes, légumineuses et aliments peu transformés.</p></div>}</div></article>}
