import{createSupabaseServerClient}from"@/lib/supabase/server";export default async function Page({params}:{params:Promise<{code:string}>}){const{code}=await params,db=await createSupabaseServerClient();const{data}=await db.rpc("academic_verify_official_document",{p_code:code});const d=data?.[0];return <main className="min-h-screen bg-[#063D2E] p-6"><section className="mx-auto mt-16 max-w-xl rounded-[32px] border-[12px] border-[#0B5D3B] bg-white p-8"><p className="font-black text-[#F47B35]">NutVitaGlobalis Academy</p><h1 className="mt-3 text-3xl font-black text-[#063D2E]">Vérification académique / Academic verification</h1>{d?<div className="mt-6 grid gap-3 rounded-2xl bg-emerald-50 p-5"><p className="font-black text-emerald-700">Document authentique / Authentic document</p><p><b>Type:</b> {d.document_type}</p><p><b>N°:</b> {d.document_number}</p><p><b>Date:</b> {d.issued_on}</p><p><b>Statut / Status:</b> {d.status}</p></div>:<p className="mt-6 rounded-2xl bg-red-50 p-5 font-bold text-red-700">Document introuvable, non publié ou révoqué / Document not found, unpublished or revoked.</p>}</section></main>}