import{NextResponse}from"next/server";import{z}from"zod";import{getDegreeSession}from"@/lib/degree-programs/access";import{createSupabaseServerClient}from"@/lib/supabase/server";
const schema=z.object({studentId:z.uuid(),semesterId:z.uuid(),reason:z.string().trim().min(3).max(500)});
export async function POST(request:Request){const s=await getDegreeSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});if(!s.isSuperAdmin&&!s.permissions.includes("result.calculate"))return NextResponse.json({error:"Forbidden"},{status:403});const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"A calculation reason is required",details:parsed.error.flatten()},{status:400});const db=await createSupabaseServerClient();const{data,error}=await db.rpc("academic_calculate_student_semester_results",{p_student_id:parsed.data.studentId,p_semester_id:parsed.data.semesterId,p_reason:parsed.data.reason});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({result:data})}