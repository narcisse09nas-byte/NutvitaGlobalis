import AcademicReportingDashboard from"@/components/degree-programs/AcademicReportingDashboard";import{requireDegreeSession}from"@/lib/degree-programs/access";import{createSupabaseServerClient}from"@/lib/supabase/server";export default async function Page(){const s=await requireDegreeSession("reporting.read"),db=await createSupabaseServerClient();const{data}=await db.from("academic_program_reporting_v").select("*").eq("organization_id",s.organizationId);const rows=(data||[]).map((x:any)=>({id:x.program_id,programId:x.program_id,code:x.program_code,nameFr:x.name_fr,nameEn:x.name_en,students:Number(x.students_total),active:Number(x.students_active),graduates:Number(x.graduates),validated:Number(x.course_results_validated),average:Number(x.average_score),success:Number(x.success_rate),resits:Number(x.resits),internships:Number(x.internships),theses:Number(x.theses),defenses:Number(x.upcoming_defenses)}));return <AcademicReportingDashboard rows={rows} canExport={s.isSuperAdmin||s.permissions.includes("reporting.export")}/>}
