import StudentRegistry from "@/components/degree-programs/StudentRegistry";
import {requireDegreeSession} from "@/lib/degree-programs/access";
import {createSupabaseServerClient} from "@/lib/supabase/server";
export default async function StudentsPage(){const session=await requireDegreeSession("student.read");const supabase=await createSupabaseServerClient();if(!session.organizationId)return <p>Organisation requise / Organization required</p>;const{data}=await supabase.from("academic_students").select("id,student_number,admission_year,current_level,current_semester,academic_status,registration_status,profile:profiles!academic_students_user_id_fkey(full_name,email),program:academic_programs(code,name_fr,name_en),campus:academic_campuses(code,name_fr)").eq("organization_id",session.organizationId).order("student_number");return <StudentRegistry rows={(data||[]) as never[]} canExport={session.isSuperAdmin||session.permissions.includes("registry.export")} organizationId={session.organizationId}/>}
