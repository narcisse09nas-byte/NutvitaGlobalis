"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {Menu,School,X} from "lucide-react";import {useState} from "react";import {degreeNavigation} from "@/config/degree-programs";import {useLanguage} from "@/hooks/use-language";import type{DegreeSession}from "@/types/degree-programs";
export default function DegreeProgramShell({session,children}:{session:DegreeSession;children:React.ReactNode}){const{locale,text}=useLanguage();const pathname=usePathname();const[open,setOpen]=useState(false);const visibleNavigation=degreeNavigation.filter(item=>!item.permission||session.isSuperAdmin||session.permissions.includes(item.permission));return <div className="min-h-screen bg-[#f4f7f5]"><header className="sticky top-0 z-40 border-b border-emerald-100 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-[1600px] items-center justify-between px-4 py-3 lg:px-8"><div className="flex items-center gap-3"><button onClick={()=>setOpen(!open)} className="rounded-xl border p-2 lg:hidden" aria-label="Menu">{open?<X/>:<Menu/>}</button><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-[#0B5D3B] text-white"><School/></div><div><p className="font-black text-[#0B5D3B]">NutVitaGlobalis Academy</p><p className="text-xs font-bold uppercase tracking-[.18em] text-[#F58220]">{text("Formation diplomante","Degree Programs")}</p></div></div><div className="text-right"><p className="text-sm font-bold text-slate-700">{session.fullName}</p><p className="text-xs text-slate-500">{session.organizationName||text("Configuration institutionnelle","Institution setup")}</p></div></div></header><div className="mx-auto grid max-w-[1600px] lg:grid-cols-[280px_1fr]"><aside className={`${open?"block":"hidden"} border-r border-emerald-100 bg-[#083f30] p-4 text-white lg:block lg:min-h-[calc(100vh-69px)]`}><nav className="grid gap-1">{visibleNavigation.map(item=>{const active=item.href==="/degree-programs"?pathname===item.href:pathname.startsWith(item.href);const Icon=item.icon;return <Link key={item.href} href={item.href} onClick={()=>setOpen(false)} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold ${active?"bg-white text-[#0B5D3B]":"text-emerald-50 hover:bg-white/10"}`}><Icon className="h-4 w-4"/><span className="flex-1">{locale==="fr"?item.fr:item.en}</span>{item.planned&&<span className="rounded-full bg-white/10 px-2 py-0.5 text-[9px] uppercase">{text("A venir","Planned")}</span>}</Link>})}</nav></aside><main className="min-w-0 p-4 lg:p-8">{children}</main></div></div>}