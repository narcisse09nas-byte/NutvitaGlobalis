"use client";import{useState}from"react";import{CheckCircle2}from"lucide-react";import{useLanguage}from"@/hooks/use-language";
export default function LessonProgressButton({lessonId,enrollmentId,completed}:{lessonId:string;enrollmentId:string;completed:boolean}){const{text}=useLanguage();const[busy,setBusy]=useState(false);async function complete(){setBusy(true);const r=await fetch("/api/degree-programs/learning/progress",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({lessonId,courseEnrollmentId:enrollmentId,status:"COMPLETED",progressPercent:100,timeSpentSeconds:0})});setBusy(false);if(r.ok)location.reload()}return <button onClick={complete} disabled={busy||completed} className="inline-flex items-center gap-2 rounded-full bg-[#0B5D3B] px-4 py-2 text-xs font-bold text-white disabled:bg-emerald-100 disabled:text-emerald-800"><CheckCircle2 className="h-4 w-4"/>{completed?text("Terminé","Completed"):busy?text("Enregistrement...","Saving..."):text("Marquer comme terminé","Mark complete")}</button>}