"use client";
import {FormEvent,useEffect,useState} from "react";
import {useTenant} from "@/hooks/use-tenant";
export function OrganizationSettingsForm(){const{activeOrganization,updateOrganization,can}=useTenant();const[name,setName]=useState("");const[academyName,setAcademyName]=useState("");const[description,setDescription]=useState("");const[primaryColor,setPrimaryColor]=useState("#0B5D3B");const[secondaryColor,setSecondaryColor]=useState("#F58220");useEffect(()=>{if(!activeOrganization)return;setName(activeOrganization.name);setAcademyName(activeOrganization.branding.academyName);setDescription(activeOrganization.description);setPrimaryColor(activeOrganization.branding.primaryColor);setSecondaryColor(activeOrganization.branding.secondaryColor);},[activeOrganization]);const organization=activeOrganization;if(!organization)return null;const branding=organization.branding;function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();updateOrganization({name,description,branding:{...branding,academyName,primaryColor,secondaryColor}})}return <form onSubmit={submit} className="rounded-[28px] border border-green-100 bg-white p-7"><div className="grid gap-5 md:grid-cols-2"><input value={name} onChange={e=>setName(e.target.value)} disabled={!can("organization.update")} className="h-12 rounded-2xl border border-slate-200 px-4"/><input value={academyName} onChange={e=>setAcademyName(e.target.value)} disabled={!can("organization.update")} className="h-12 rounded-2xl border border-slate-200 px-4"/><textarea value={description} onChange={e=>setDescription(e.target.value)} disabled={!can("organization.update")} rows={4} className="rounded-2xl border border-slate-200 px-4 py-3 md:col-span-2"/><input type="color" value={primaryColor} onChange={e=>setPrimaryColor(e.target.value)} className="h-12 w-full rounded-2xl border p-2"/><input type="color" value={secondaryColor} onChange={e=>setSecondaryColor(e.target.value)} className="h-12 w-full rounded-2xl border p-2"/></div>{can("organization.update")&&<button className="mt-7 rounded-full bg-[#F58220] px-6 py-3 font-bold text-white">Enregistrer</button>}</form>}
