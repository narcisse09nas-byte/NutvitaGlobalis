"use client";
import { useState } from "react";
import Link from "next/link";
type Plan={id:string;name:string;name_en?:string;price_excluding_tax?:number;amount?:number;currency?:string;service_type:string;tier:string;features?:string[]};
export default function HealthOfferCards({plans,english,userConnected,access}:{plans:Plan[];english:boolean;userConnected:boolean;access:{health?:boolean;childGrowth?:boolean}|null}){const[selected,setSelected]=useState<Plan|null>(null);const t=(fr:string,en:string)=>english?en:fr;function active(plan:Plan){return plan.service_type==="child_growth"?access?.childGrowth:access?.health}function destination(plan:Plan){if(active(plan))return `/api/access/open?service=${plan.service_type==="child_growth"?"child_growth":"health"}&role=client`;const checkout=`/checkout?type=subscription&id=${plan.id}`;return userConnected?checkout:`/connexion?redirect=${encodeURIComponent(checkout)}`}
return <><div className="mx-auto mt-9 grid max-w-6xl gap-6 md:grid-cols-2 xl:grid-cols-4">{plans.map(plan=>{const isActive=active(plan),premium=String(plan.tier).toLowerCase()==="premium",child=plan.service_type==="child_growth";return <article key={plan.id} className={`flex min-h-full flex-col rounded-3xl border bg-white p-8 shadow-soft ${premium?"border-orange":"border-white"}`}><div className="flex items-center justify-between gap-3"><p className={`text-xs font-black uppercase tracking-widest ${premium?"text-orange":"text-leaf"}`}>{premium?"Premium":"Standard"}</p><span className={`rounded-full px-3 py-1 text-xs font-black ${isActive?"bg-emerald-100 text-emerald-800":"bg-slate-100 text-slate-600"}`}>{isActive?t("Actif","Active"):t("Disponible","Available")}</span></div><h3 className="mt-4 text-2xl font-black">{english&&plan.name_en?plan.name_en:plan.name}</h3><p className="mt-4 flex-1 leading-7 text-slate-600">{child?t("Dossier privé par enfant, courbes, historique et analyses adaptées.","A private record per child with charts, history and tailored analysis."):premium?t("Analyses avancées, rapports enrichis et accompagnement numérique renforcé.","Advanced analysis, enhanced reports and stronger digital guidance."):t("Mesures, tendances, questionnaires et rapports personnalisés.","Measurements, trends, questionnaires and personalized reports.")}</p>{plan.features?.length?<ul className="mt-5 grid gap-2 text-sm">{plan.features.slice(0,4).map(feature=><li key={feature}>✓ {feature}</li>)}</ul>:null}<button type="button" onClick={()=>isActive?location.assign(destination(plan)):setSelected(plan)} className="btn-primary mt-7">{isActive?t("Ouvrir le service","Open the service"):t("Activer le service","Activate the service")}</button></article>})}</div>{selected&&<div className="fixed inset-0 z-[120] grid place-items-center overflow-y-auto bg-slate-950/60 p-4" role="dialog" aria-modal="true"><section className="w-full max-w-lg rounded-[2rem] bg-white p-7 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-widest text-orange">{String(selected.tier).toLowerCase()==="premium"?"Premium":"Standard"}</p><h2 className="mt-2 text-3xl font-black">{english&&selected.name_en?selected.name_en:selected.name}</h2></div><button type="button" onClick={()=>setSelected(null)} className="grid h-10 w-10 place-items-center rounded-full bg-slate-100 text-xl" aria-label={t("Fermer","Close")}>×</button></div><p className="mt-6 text-sm text-slate-500">{t("Montant du service","Service price")}</p><p className="mt-1 text-4xl font-black text-orange">{Number(selected.price_excluding_tax??selected.amount??0).toLocaleString("fr-FR")} {selected.currency||"XOF"}</p><p className="mt-4 rounded-2xl bg-mint p-4 text-sm leading-6 text-forest">{userConnected?t("Votre compte sera utilisé pour cette activation. Aucun nouveau compte ne sera créé.","Your existing account will be used. No new account will be created."):t("Connectez-vous ou créez votre compte pour poursuivre l'achat.","Sign in or create an account to continue your purchase.")}</p><div className="mt-7 flex flex-wrap gap-3"><Link href={destination(selected)} className="btn-primary">{userConnected?t("Continuer vers l'achat","Continue to purchase"):t("Se connecter et continuer","Sign in and continue")}</Link><button type="button" onClick={()=>setSelected(null)} className="btn-secondary">{t("Annuler","Cancel")}</button></div></section></div>}</>}
