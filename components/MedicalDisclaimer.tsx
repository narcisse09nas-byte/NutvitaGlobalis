"use client";
import { usePathname } from "next/navigation";
import { useState } from "react";
export default function MedicalDisclaimer(){const[visible,setVisible]=useState(true),english=(usePathname()||"").startsWith("/en");if(!visible)return null;return <aside className="fixed bottom-4 right-4 z-[90] max-w-[320px] rounded-2xl border border-orange/15 bg-white/95 p-4 text-xs leading-5 text-slate-600 shadow-xl backdrop-blur" role="note"><button type="button" onClick={()=>setVisible(false)} aria-label={english?"Close":"Fermer"} className="absolute right-2 top-2 grid h-7 w-7 place-items-center rounded-full bg-slate-100 text-slate-500">×</button><span className="mr-2 font-black text-orange" aria-hidden="true">ⓘ</span><span className="pr-5">{english?"NutVitaGlobalis information does not replace urgent medical care or a physician's advice.":"Les informations de NutVitaGlobalis ne remplacent pas les soins médicaux urgents ni l'avis d'un médecin."}</span></aside>}
