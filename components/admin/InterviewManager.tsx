"use client";
import {FormEvent,useState} from "react";import VideoRoom from "@/components/recruitment/VideoRoom";import RecruitmentChat from "@/components/recruitment/RecruitmentChat";
type Row=Record<string,any>;const criteria=[['technical_skill','Compétence technique'],['communication','Communication'],['professional_ethics','Éthique professionnelle'],['clinical_experience','Expérience clinique'],['teleconsultation_aptitude','Aptitude au téléconseil'],['availability','Disponibilité'],['motivation','Motivation']] as const;
export default function InterviewManager({applications,initial,adminId}:{applications:Row[];initial:Row[];adminId:string}){const [interviews,setInterviews]=useState(initial),[selected,setSelected]=useState<Row|null>(null),[form,setForm]=useState({application_id:'',scheduled_at:'',duration_minutes:45}),[message,setMessage]=useState('');async function schedule(e:FormEvent){e.preventDefault();const response=await fetch('/api/recruitment/interview',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const result=await response.json();if(!response.ok){setMessage(result.message);return}setInterviews([result.interview,...interviews]);setMessage('Entretien planifié et invitation envoyée.')}async function evaluate(complete=false){if(!selected)return;const evaluation=selected.interview_evaluations?.[0]||{};const response=await fetch('/api/recruitment/interview',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({...evaluation,interview_id:selected.id,admin_notes:selected.admin_notes,complete})});const result=await response.json();setMessage(response.ok?'Évaluation enregistrée.':result.message)}function setEvaluation(name:string,value:unknown){const evaluation={...(selected?.interview_evaluations?.[0]||{}),[name]:value};setSelected({...selected,interview_evaluations:[evaluation]})}return <div><form onSubmit={schedule} className="mb-8 grid gap-4 rounded-2xl border bg-white p-6 md:grid-cols-4"><label className="grid gap-2 text-sm font-bold md:col-span-2">Candidat<select required className="admin-input" value={form.application_id} onChange={e=>setForm({...form,application_id:e.target.value})}><option value="">Sélectionner…</option>{applications.map(a=><option key={a.id} value={a.id}>{a.full_name} · {a.email}</option>)}</select></label><label className="grid gap-2 text-sm font-bold">Date et heure<input required type="datetime-local" className="admin-input" value={form.scheduled_at} onChange={e=>setForm({...form,scheduled_at:e.target.value})}/></label><label className="grid gap-2 text-sm font-bold">Durée (minutes)<input type="number" className="admin-input" value={form.duration_minutes} onChange={e=>setForm({...form,duration_minutes:Number(e.target.value)})}/></label><button className="btn-primary justify-self-start md:col-span-4">Planifier et inviter</button></form>{message&&<p className="mb-5 rounded-xl bg-mint p-4 font-bold text-forest">{message}</p>}<div className="grid gap-4">{interviews.map(i=><button key={i.id} onClick={()=>setSelected(i)} className="rounded-2xl border bg-white p-5 text-left"><div className="flex flex-wrap justify-between gap-3"><div><b className="text-lg text-forest">{i.recruitment_applications?.full_name||'Candidat'}</b><p className="text-sm text-slate-500">{new Date(i.scheduled_at).toLocaleString('fr-FR')} · {i.duration_minutes} min</p></div><span className="rounded-full bg-mint px-3 py-1 text-xs font-bold text-leaf">{i.status}</span></div></button>)}{!interviews.length&&<p className="rounded-2xl bg-white p-8 text-center text-slate-400">Aucun entretien planifié.</p>}</div>{selected&&<div className="fixed inset-0 z-[100] overflow-y-auto bg-slate-950/70 p-3"><div className="mx-auto my-4 max-w-7xl rounded-3xl bg-slate-100 p-5"><div className="mb-5 flex justify-between"><div><h2 className="text-2xl font-black">Entretien de {selected.recruitment_applications?.full_name}</h2><p>{new Date(selected.scheduled_at).toLocaleString('fr-FR')}</p></div><button onClick={()=>setSelected(null)} className="text-3xl">×</button></div><VideoRoom roomName={selected.room_name} displayName="Équipe NutVitaGlobalis"/><div className="mt-6 grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border bg-white p-6"><h3 className="text-xl font-black">Grille d’évaluation</h3><div className="mt-5 grid gap-4 sm:grid-cols-2">{criteria.map(([name,label])=><label key={name} className="grid gap-2 text-sm font-bold">{label}<select className="admin-input" value={selected.interview_evaluations?.[0]?.[name]||''} onChange={e=>setEvaluation(name,Number(e.target.value))}><option value="">Non noté</option>{[1,2,3,4,5].map(x=><option key={x}>{x}</option>)}</select></label>)}</div><label className="mt-4 grid gap-2 text-sm font-bold">Notes internes<textarea rows={4} className="admin-input" value={selected.admin_notes||''} onChange={e=>setSelected({...selected,admin_notes:e.target.value})}/></label><label className="mt-4 grid gap-2 text-sm font-bold">Recommandation<select className="admin-input" value={selected.interview_evaluations?.[0]?.recommendation||'pending'} onChange={e=>setEvaluation('recommendation',e.target.value)}><option value="pending">À décider</option><option value="select">Recommander</option><option value="reject">Ne pas recommander</option></select></label><div className="mt-5 flex gap-3"><button onClick={()=>evaluate(false)} className="btn-secondary">Enregistrer</button><button onClick={()=>evaluate(true)} className="btn-primary">Terminer l’entretien</button></div></section><RecruitmentChat applicationId={selected.application_id} currentUserId={adminId} candidateId={selected.candidate_id} isAdmin/></div></div></div>}</div>}
