"use client";
type Subscriber={id:string;email:string;source:string;subscribed_at:string};
export default function NewsletterTable({rows}:{rows:Subscriber[]}){function exportCsv(){const csv=['Email,Source,Date',...rows.map(r=>`"${r.email}","${r.source}","${new Date(r.subscribed_at).toISOString()}"`)].join('\n');const url=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='newsletter-nutvita.csv';a.click();URL.revokeObjectURL(url)}return <><div className="mb-5 flex justify-end"><button onClick={exportCsv} className="btn-primary">Exporter CSV</button></div><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full"><thead className="border-b bg-slate-50 text-left text-xs uppercase text-slate-400"><tr><th className="p-4">Email</th><th className="p-4">Source</th><th className="p-4">Inscription</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-b"><td className="p-4 font-bold text-forest">{r.email}</td><td className="p-4">{r.source}</td><td className="p-4">{new Date(r.subscribed_at).toLocaleString('fr-FR')}</td></tr>)}{!rows.length&&<tr><td colSpan={3} className="p-8 text-center text-slate-400">Aucun inscrit.</td></tr>}</tbody></table></div></>}
