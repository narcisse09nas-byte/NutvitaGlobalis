"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";import SignupFields from "@/components/accounts/SignupFields";
export default function ClientAuth(){const [mode,setMode]=useState<'login'|'signup'>('login'),[message,setMessage]=useState(''),[loading,setLoading]=useState(false),router=useRouter();async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setMessage('');const fd=new FormData(event.currentTarget),email=String(fd.get('email')),password=String(fd.get('password')),supabase=createClient();if(mode==='login'){const result=await supabase.auth.signInWithPassword({email,password});if(result.error){setMessage(result.error.message);setLoading(false);return}router.refresh();return}if(password!==String(fd.get('password_confirmation'))){setMessage('Les mots de passe ne correspondent pas.');setLoading(false);return}const profile={full_name:String(fd.get('full_name')),whatsapp_phone:String(fd.get('whatsapp_phone')),country:String(fd.get('country')),country_code:String(fd.get('country_code')),state_region:String(fd.get('state_region')),city:String(fd.get('city'))==='__other'?String(fd.get('other_city')):String(fd.get('city')),other_city:String(fd.get('other_city')||''),account_type:'client',accepted_terms_at:new Date().toISOString(),accepted_privacy_at:new Date().toISOString()};const result=await supabase.auth.signUp({email,password,options:{data:profile,emailRedirectTo:`${location.origin}/espace-client`}});if(result.error){setMessage(result.error.message);setLoading(false);return}if(result.data.user&&result.data.session)await supabase.from('client_profiles').upsert({id:result.data.user.id,email,...profile});setMessage(result.data.session?'Compte cree.':'Compte cree. Confirmez votre adresse email.');setLoading(false);if(result.data.session)router.refresh()}
return <div className={`w-full ${mode==='signup'?'max-w-2xl':'max-w-md'} rounded-3xl bg-white p-8 shadow-2xl`}><h1 className="text-3xl font-black">Espace client</h1><p className="mt-2 text-slate-500">{mode==='login'?'Accedez a votre suivi sante.':'Creez votre espace sante securise.'}</p><form onSubmit={submit} className="mt-7 grid gap-4">{mode==='signup'?<SignupFields/>:<><label className="grid gap-2 text-sm font-bold">Email<input name="email" type="email" required className="admin-input"/></label><label className="grid gap-2 text-sm font-bold">Mot de passe<input name="password" type="password" minLength={8} required className="admin-input"/></label></>}{message&&<p className="rounded-xl bg-mint p-3 text-sm">{message}</p>}<button disabled={loading} className="btn-primary">{loading?'Patientez...':mode==='login'?'Se connecter':'Creer mon espace'}</button></form><button onClick={()=>{setMode(mode==='login'?'signup':'login');setMessage('')}} className="mt-5 w-full text-sm font-bold text-leaf">{mode==='login'?'Creer un compte client':'J ai deja un compte'}</button></div>}

