"use client";import {FormEvent,useState} from "react";export default function PasswordChange(){const [message,setMessage]=useState(''),[ok,setOk]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget)),response=await fetch('/api/client/change-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),result=await response.json();setOk(response.ok);setMessage(response.ok?'Mot de passe modifié.':result.message);if(response.ok)e.currentTarget.reset()}return <form onSubmit={submit} className="grid max-w-xl gap-4 rounded-2xl border bg-white p-6"><label className="grid gap-2 text-sm font-bold">Ancien mot de passe<input name="old_password" type="password" required className="admin-input"/></label><label className="grid gap-2 text-sm font-bold">Nouveau mot de passe<input name="new_password" type="password" minLength={8} required className="admin-input"/></label><label className="grid gap-2 text-sm font-bold">Confirmation<input name="confirmation" type="password" minLength={8} required className="admin-input"/></label><button className="btn-primary justify-self-start">Modifier</button>{message&&<p className={ok?'text-leaf':'text-red-600'}>{message}</p>}</form>}
