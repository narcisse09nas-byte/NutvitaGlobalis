"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {displayMoney,formatUsd,xofToUsd} from "@/lib/currency";
type Row=Record<string,any>;const periodLabel=(months:number)=>months===1?'Mensuel':months===3?'Trimestriel':'Annuel';
export default function SubscriptionPlans({plans,current,taxRate,invoices=[]}:{plans:Row[];current:Row|null;taxRate:number;invoices?:Row[]}){const [loading,setLoading]=useState(''),[message,setMessage]=useState('');function checkout(plan:string){setLoading(plan);location.href=`/checkout?type=subscription&id=${encodeURIComponent(plan)}`}async function invoice(path:string){const {data,error}=await createClient().storage.from('document-vault').createSignedUrl(path,180);if(error)setMessage(error.message);else window.open(data.signedUrl,'_blank')}return <div className="grid gap-6">{current&&<div className="rounded-2xl bg-mint p-5"><b>Abonnement actuel : {current.subscription_plans?.name||current.plan_id}</b><div className="mt-2 grid gap-1 text-sm sm:grid-cols-3"><span>Statut : {current.status}</span><span>Début : {current.started_at?new Date(current.started_at).toLocaleDateString('fr-FR'):'En attente'}</span><span>Expiration : {current.expires_at?new Date(current.expires_at).toLocaleDateString('fr-FR'):'En attente'}</span></div></div>}<p className="text-sm text-slate-500">Tous les montants sont facturés en USD. Le moyen de paiement est choisi au checkout.</p>{message&&<p className="rounded-xl bg-amber-50 p-4 text-amber-900">{message}</p>}<div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{plans.map(plan=>{const ht=Number(plan.price_excluding_tax??plan.amount),tax=Math.round(ht*taxRate)/100,total=ht+tax,duration=Number(plan.duration_months||12),period=periodLabel(duration);return <article key={plan.id} className={`rounded-3xl border bg-white p-6 ${plan.tier==='premium'?'border-orange shadow-soft':''}`}><p className="text-xs font-bold uppercase text-leaf">{plan.tier==='premium'?'Premium':'Basic'} · {period}</p><h2 className="mt-2 text-2xl font-black">{plan.name}</h2><div className="my-5 grid gap-2 rounded-2xl bg-slate-50 p-4 text-sm"><Line label="Prix HT" value={formatUsd(xofToUsd(ht))}/><Line label={`Taxe (${taxRate} %)`} value={formatUsd(xofToUsd(tax))}/><Line label="Total TTC" value={formatUsd(xofToUsd(total))} strong/><Line label="Durée" value={`${duration} mois`}/><Line label="Renouvellement" value={period}/></div><ul className="my-5 grid gap-2 text-sm">{(plan.features||[]).map((feature:string)=><li key={feature}>✓ {feature}</li>)}</ul><button disabled={loading===plan.id} onClick={()=>checkout(plan.id)} className="btn-primary w-full">{loading===plan.id?'Redirection...':'Payer et activer'}</button></article>})}</div>{invoices.length>0&&<section><h2 className="mb-3 text-xl font-black">Mes factures</h2><div className="grid gap-3">{invoices.map(item=><button key={item.id} onClick={()=>invoice(item.file_path)} className="flex justify-between rounded-xl border bg-white p-4 text-left"><span>{item.invoice_number}</span><b>{displayMoney(Number(item.total_including_tax),item.currency)}</b></button>)}</div></section>}</div>}
function Line({label,value,strong=false}:{label:string;value:string;strong?:boolean}){return <div className={`flex justify-between ${strong?'border-t pt-2 text-lg':''}`}><span>{label}</span><b className={strong?'text-forest':''}>{value}</b></div>}
