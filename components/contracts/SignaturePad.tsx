"use client";
import {useEffect,useRef,useState} from "react";
export default function SignaturePad({onChange}:{onChange:(blob:Blob|null)=>void}){const canvas=useRef<HTMLCanvasElement>(null),drawing=useRef(false),[empty,setEmpty]=useState(true);useEffect(()=>{const c=canvas.current;if(!c)return;const ratio=Math.max(window.devicePixelRatio||1,1),rect=c.getBoundingClientRect();c.width=rect.width*ratio;c.height=180*ratio;const ctx=c.getContext('2d');ctx?.scale(ratio,ratio);if(ctx){ctx.lineWidth=2.2;ctx.lineCap='round';ctx.strokeStyle='#123c2f'}},[]);function point(e:React.PointerEvent<HTMLCanvasElement>){const rect=e.currentTarget.getBoundingClientRect();return{x:e.clientX-rect.left,y:e.clientY-rect.top}}function start(e:React.PointerEvent<HTMLCanvasElement>){drawing.current=true;e.currentTarget.setPointerCapture(e.pointerId);const p=point(e),ctx=e.currentTarget.getContext('2d');ctx?.beginPath();ctx?.moveTo(p.x,p.y)}function move(e:React.PointerEvent<HTMLCanvasElement>){if(!drawing.current)return;const p=point(e),ctx=e.currentTarget.getContext('2d');ctx?.lineTo(p.x,p.y);ctx?.stroke();setEmpty(false)}function end(){drawing.current=false;canvas.current?.toBlob(blob=>onChange(blob),'image/png')}function clear(){const c=canvas.current;if(!c)return;c.getContext('2d')?.clearRect(0,0,c.width,c.height);setEmpty(true);onChange(null)}return <div><canvas ref={canvas} onPointerDown={start} onPointerMove={move} onPointerUp={end} onPointerCancel={end} className="h-[180px] w-full touch-none rounded-xl border-2 border-dashed border-slate-300 bg-white"/><div className="mt-2 flex justify-between text-xs text-slate-400"><span>Signez au doigt, à la souris ou au stylet</span><button type="button" onClick={clear} disabled={empty} className="font-bold text-red-600 disabled:opacity-40">Effacer</button></div></div>}
